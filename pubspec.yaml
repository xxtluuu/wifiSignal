name: wifisignal
description: WiFi信号测试工具
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  wifi_iot: ^0.3.18
  network_info_plus: ^4.0.0
  fl_chart: ^0.65.0
  provider: ^6.0.5
  vibration: ^1.8.3
  get_it: ^7.6.7
  ping_discover_network_forked: ^0.0.1
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
